<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="10dp"
    android:paddingEnd="0dp"
    style="@style/RootLayout">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/text_view_message"
            style="@style/Title"
            android:textSize="16sp"
            tools:text="Удалить хранилище?"/>

        <CheckedTextView
            android:id="@+id/check_box_is_with_files"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="end|center_vertical"
            android:checkMark="@drawable/checkbox_selector_red"
            android:text="@string/label_delete_storage_with_files"
            android:textColor="@color/error_1"
            android:textSize="16sp"
            style="@style/AppCheckbox"/>

    </LinearLayout>

</ScrollView>